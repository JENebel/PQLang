n = 0;
a = 0;

while (n < 1000) {
	if((n != 0) && (n != 1)) {
		i = (£n);
		g = true;
		while(i > 1){
			if((!g) || ((n % i) == 0)) {
				g = false
			};
			i--;
		};
		if(g) {
			a++;
		}
	};
	n++;
};

if(a > 100) {print("Kæft jeg er Go'")} else {print("Så godt}}\"")};

print("Primes found: " + a)